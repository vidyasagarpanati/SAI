# Pinned for reproducibility. Windows 10/11, Python 3.11 or 3.12.
mediapipe==0.10.21
# IMPORTANT: mediapipe depends on opencv-contrib-python. Do NOT also install
# opencv-python or opencv-python-headless. All three install into the same
# site-packages/cv2 directory, and on Windows the mismatched native DLLs make
# `from mediapipe.tasks.python import vision` fail with
#   ImportError: DLL load failed while importing _framework_bindings
opencv-contrib-python==4.11.0.86
numpy==1.26.4
pandas==2.2.3
pyarrow==17.0.0
imageio-ffmpeg==0.5.1
jinja2==3.1.4
pydantic==2.9.2
jsonschema==4.23.0
PyYAML==6.0.2
httpx==0.27.2
langgraph==0.2.45
langgraph-checkpoint-sqlite==2.0.1
rich==13.9.4
pytest==8.3.3
